<section
  class="w-full flex flex-col gap-5 justify-center items-center px-4 py-32 mb-6"
>
  <div class="flex flex-col gap-2 justify-center items-center">
    <h1
      class="w-full max-w-4xl sm:text-center text-3xl sm:text-5xl md:text-7xl font-medium tracking-tighter text-pretty"
    >
      Bringing innovation to life with heart and purpose.
    </h1>
    <p
      class=" w-full max-w-2xl text-muted-foreground sm:text-center text-normal sm:text-lg md:text-xl font-light tracking-tighter text-balance"
    >
      Meet Rana Jollanar, a passionate creator blending XR and AI to craft visionary experiences. 
    </p>
  </div>
  <div class="w-full flex flex-wrap gap-3 items-center justify-center">
    <a
      href="https://www.linkedin.com/in/rana-jollanar-ben-hassine/"
      target="_blank"
      rel="noopener noreferrer"
      class="relative w-fit flex justify-center items-center px-4 py-2 sm:px-6 sm:py-3 sm:text-lg font-medium text-background bg-primary rounded-full perfect-shadow"
    >
      Connect with me
      <div
        class="z-[-1] bg-gradient-to-b from-secondary via-primary to-primary absolute w-[calc(100%-4px)] h-[calc(100%-4px)] rounded-full border-t-2 border-secondary"
      >
      </div>
    </a>
  </div>
</section>

<style>
  @keyframes heroIn {
    0% {
      opacity: 0;
      transform: translateY(0.5rem);
    }
    60% {
      opacity: 1;
      transform: translateY(-0.05rem);
    }
    100% {
      opacity: 1;
      transform: translateY(0);
    }
  }

  h1 {
    opacity: 0;
    animation: heroIn 0.7s cubic-bezier(0.5, 1.15, 0.2, 1.1) forwards;
  }
  p {
    opacity: 0;
    animation: heroIn 0.7s cubic-bezier(0.5, 1.15, 0.2, 1.1) 0.3s forwards;
  }
  a {
    opacity: 0;
    animation: heroIn 0.7s cubic-bezier(0.5, 1.15, 0.2, 1.1) 0.6s forwards;
  }
</style>
